<h2>{{title}}</h2>
<a href="/add/gadget">➕ Add New Gadget</a>
<ul>
  {{#each gadgets}}
    <li>
      <strong>{{name}}</strong> ({{class}})
      <br>
      {{description}}
      {{#if image_url}}
        <br><img src="{{image_url}}" alt="{{name}}" width="150">
      {{/if}}
    </li>
  {{/each}}
</ul>
